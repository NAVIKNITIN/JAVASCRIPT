let keys = Object.keys(candidateDetails?.refereePreferencesResponse);
keys.forEach((key, index) => {
  // console.log(
  //   `${key}: ${candidateDetails?.refereePreferencesResponse[key]}`
  // );
});
